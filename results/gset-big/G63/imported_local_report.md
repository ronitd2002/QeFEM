# G63: imported older local baseline

This 128-replica Adam run reached cut `26753` against reference `27047` (gap `294`). No per-step history was present in the local archive.

It is retained for workflow continuity and is not presented as a Feng-tuned result.
