# G60: imported older local baseline

This 128-replica Adam run reached cut `14037` against reference `14188` (gap `151`). No per-step history was present in the local archive.

It is retained for workflow continuity and is not presented as a Feng-tuned result.
