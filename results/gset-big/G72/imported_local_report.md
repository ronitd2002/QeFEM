# G72: imported older local baseline

This 128-replica Adam run reached cut `6860` against reference `7008` (gap `148`). No per-step history was present in the local archive.

It is retained for workflow continuity and is not presented as a Feng-tuned result.
