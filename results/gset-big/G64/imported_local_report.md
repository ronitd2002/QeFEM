# G64: imported older local baseline

This 128-replica Adam run reached cut `8415` against reference `8751` (gap `336`). No per-step history was present in the local archive.

It is retained for workflow continuity and is not presented as a Feng-tuned result.
