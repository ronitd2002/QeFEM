# G5: imported older local baseline

This 128-replica Adam run reached cut `11622` against reference `11631` (gap `9`). A normalized historical trajectory is available as `imported_local_trajectory.csv`.

It is retained for workflow continuity and is not presented as a Feng-tuned result.
