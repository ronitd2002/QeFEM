# G21: imported older local baseline

This 128-replica Adam run reached cut `915` against reference `931` (gap `16`). A normalized historical trajectory is available as `imported_local_trajectory.csv`.

It is retained for workflow continuity and is not presented as a Feng-tuned result.
