# G52: imported older local baseline

This 128-replica Adam run reached cut `3825` against reference `3851` (gap `26`). No per-step history was present in the local archive.

It is retained for workflow continuity and is not presented as a Feng-tuned result.
