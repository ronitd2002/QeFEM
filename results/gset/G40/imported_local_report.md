# G40: imported older local baseline

This 128-replica Adam run reached cut `2296` against reference `2400` (gap `104`). No per-step history was present in the local archive.

It is retained for workflow continuity and is not presented as a Feng-tuned result.
