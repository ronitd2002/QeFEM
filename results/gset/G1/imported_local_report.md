# G1: imported older local baseline

This 128-replica Adam run reached cut `11611` against reference `11624` (gap `13`). A normalized historical trajectory is available as `imported_local_trajectory.csv`.

It is retained for workflow continuity and is not presented as a Feng-tuned result.
