# G41: imported older local baseline

This 128-replica Adam run reached cut `2289` against reference `2405` (gap `116`). No per-step history was present in the local archive.

It is retained for workflow continuity and is not presented as a Feng-tuned result.
