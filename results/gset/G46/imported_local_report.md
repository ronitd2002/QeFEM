# G46: imported older local baseline

This 128-replica Adam run reached cut `6627` against reference `6649` (gap `22`). No per-step history was present in the local archive.

It is retained for workflow continuity and is not presented as a Feng-tuned result.
