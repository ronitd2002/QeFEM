# G28: imported older local baseline

This 128-replica Adam run reached cut `3275` against reference `3298` (gap `23`). A normalized historical trajectory is available as `imported_local_trajectory.csv`.

It is retained for workflow continuity and is not presented as a Feng-tuned result.
