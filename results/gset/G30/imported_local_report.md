# G30: imported older local baseline

This 128-replica Adam run reached cut `3367` against reference `3413` (gap `46`). No per-step history was present in the local archive.

It is retained for workflow continuity and is not presented as a Feng-tuned result.
